//literal pattern 

let employee = {
    id: 1,
    name: 'Subramanian',
    city: 'Coimbatore',
    calculateSalary() {
        return 100
    }
}
console.log(employee.id, employee.name, employee.calculateSalary())